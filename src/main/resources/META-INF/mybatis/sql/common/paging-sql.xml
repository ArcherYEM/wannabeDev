<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="common.sql.paging">
    <!-- 페이징을 위한 SQL 조각 -->
    <sql id="start">
        SELECT
              ZZ.*
        FROM
            (SELECT
                  XX.*
                , ROWNUM AS RN
            FROM (
    </sql>

    <!-- 페이징을 위한 SQL 조각 -->
    <sql id="end">
                ) XX
            ) ZZ
        WHERE ZZ.RN BETWEEN ((#{page}-1) * #{size}) + 1 AND #{page} * #{size}
    </sql>
</mapper>
