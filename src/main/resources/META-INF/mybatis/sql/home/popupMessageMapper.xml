<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dev.wannabe.domain.home.mapper.ImageSlideMapper">

<!--    <select id="getImageSlide" resultType="com.dev.wannabe.domain.home.model.dto.ImageSlideDTO">
        SELECT *
        FROM ATTACH_FILE_MANAGE
        WHERE FILE_ORIGIN_NAME LIKE #{imgFileName}
    </select>-->
    <!--        <select id="getImageSlide" resultType="java.util.Map">
                SELECT
                    ATTACH_FILE_ID,
                    FILE_ORIGIN_NAME,
                    FILE_NAME,
                    FILE_SIZE,
                    FILE_PATH,
                    FILE_EXTENSION,
                    REMARKS,
                    INSERT_USER_ID,
                    INSERT_DT,
                    UPDATE_USER_ID,
                    UPDATE_DT
                FROM ATTACH_FILE_MANAGE
                WHERE FILE_ORIGIN_NAME LIKE '%ggum%'
            </select>-->
</mapper>