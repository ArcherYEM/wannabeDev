<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- productMapper.xml -->
<!-- ProductMapper.xml -->
<mapper namespace="com.dev.wannabe.domain.home.mapper.ProductMapper">

    <select id="selectTop3ByProductTypeSortedByInsertDt" resultType="com.dev.wannabe.domain.home.model.dto.ProductDTO">
        SELECT
            P.PRODUCT_ID,
            P.PRODUCT_NAME,
            P.PRODUCT_TYPE,
            F.FILE_PATH,
            F.FILE_NAME
        FROM PRODUCT P
                 JOIN ATTACH_FILE_MANAGE F ON P.ATTACH_FILE_ID = F.ATTACH_FILE_ID
        WHERE P.PRODUCT_TYPE = #{productType}
        ORDER BY P.INSERT_DT DESC
            LIMIT 3
    </select>

</mapper>