<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dev.wannabe.domain.minihompi.mapper.minihompi.MiniHompiMapper">

    <select id="findMyHompi" parameterType="map"
            resultType="com.dev.wannabe.domain.minihompi.model.minihompi.MiniHompiTotal">
        SELECT hp.HOMPI_ID, hp.HOMPI_URL, hp.HOMPI_TITLE, hp.OWNER_USER_ID, NVL(hds.TODAY_CNT,0)TODAY_CNT, hp.TOTAL_CNT
        FROM HOMPI hp
        LEFT OUTER JOIN HOMPI_DAILY_STATS hds
        ON hp.HOMPI_ID = hds.HOMPI_ID AND hds.DAY_STATS_DATE = CURDATE()
        WHERE 1 = 1
        <if test="hompiId == userId">
            AND hp.HOMPI_ID = #{hompiId}
            AND hp.OWNER_USER_ID = #{userId}
        </if>
        <if test="userId == null or userId != hompiId ">
            AND hp.HOMPI_ID = #{hompiId}

        </if>

    </select>

    <update id="updateTitle" parameterType="map">
        UPDATE wannabe_dev.HOMPI
        SET HOMPI_TITLE= #{title}
        WHERE HOMPI_ID = #{hompiId}

    </update>

</mapper>