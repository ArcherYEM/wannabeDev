<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dev.wannabe.domain.minihompi.mapper.MiniroomMapper">

    <insert id="saveMiniroomBasic" parameterType="MiniroomBasic">
        INSERT INTO MINIROOM_BASIC (
            USER_ID, PRODUCT_ID,
            INSERT_USER_ID
        )
        VALUES (
            #{userId},
            #{productId},
            #{insertUserId}
        )
        ON DUPLICATE KEY UPDATE
            PRODUCT_ID = #{productId},
            UPDATE_USER_ID = #{updateUserId}
    </insert>
</mapper>